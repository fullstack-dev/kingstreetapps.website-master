# King Street apps website

1. Getting setup for local development
  a) Change directory into the project root `cd ~/{path-to-this-file}`
  b) Install all required modules `npm install`
  c) Start the dev server `npm start`
